<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="@color/soccer_green"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="24dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/img_soccer_ball"
        android:contentDescription="Logo" />

    <TextView
        android:id="@+id/txtHeaderTeamName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NOMBRE EQUIPO U ORGANIZADOR"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:layout_marginTop="12dp" />

    <TextView
        android:id="@+id/txtHeaderRole"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rol de usuario"
        android:textSize="14sp"
        android:textColor="#A8E6A3"
        android:layout_marginTop="4dp" />

</LinearLayout>